# Use wildcard "*" to expose all endpoints
# Can also expose individual endpoints using comma delimited list
management.endpoints.web.exposure.include=*
#management.endpoints.web.exposure.include=health,info
management.info.env.enabled=true

# Exclude individual endpoints using comma delimited list
#management.endpoints.web.exposure.exclude=health,info

info.app.name=My Super Cool App
info.app.description=A Crazy fun app, yoo hoo!
info.app.version=1.0.0

#spring.security.user.name=scott
#spring.security.user.password=tiger

# define custom properties
coach.name=Mickey Mouse
team.name=The Mouse Club

#
# Change Spring boot embedded server port
#
server.port=7070

#
# Set the context path of the application
#
# All request should be prefixed with /mycoolapp
#
server.servlet.context-path=/mycoolapp